import styled from 'styled-components';
import { fontColour, fontSize } from '../../utils/styles';

export const Input = styled.input`
  color: ${fontColour};
  margin-left: 0;
  margin-right: 5px;
`;

export const Label = styled.label`
  color: ${fontColour};
  font-size: ${fontSize};
`;
